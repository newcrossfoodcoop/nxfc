'use strict';

// This is now just a proxy file TODO: replace references with the require below

var path = require('path');
var errors = require(path.resolve('./config/lib/errors'));

module.exports = errors;
